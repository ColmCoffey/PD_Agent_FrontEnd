version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Using environment variables from Amplify console"
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
